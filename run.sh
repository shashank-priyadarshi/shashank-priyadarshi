#!/bin/bash

# run mode: container/local
#local = /bin/bash -c cd $(pwd)/cmd/ && air -c ./.air.toml
#container = /bin/bash -c cd $(pwd)/

#
# Check if docker is available
# Run using air in Docker
# If not, run locally using air
#
